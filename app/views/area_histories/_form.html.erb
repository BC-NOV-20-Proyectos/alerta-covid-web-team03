<%= form_with(model: area_history) do |form| %>
  <% if area_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(area_history.errors.count, "error") %> prohibited this area_history from being saved:</h2>

      <ul>
        <% area_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
